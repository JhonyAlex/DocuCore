import { useEffect } from 'react'
import { calendarLongDate } from '@/lib/calendarDates'
import { calendarCategoryPresentation, calendarStatusLabel } from '@/lib/calendarPresentation'
import type { ApiCalendarEventOccurrence } from '@/lib/api'

export default function CalendarEventDetails({ event, busy, error, onClose, onComplete, onEdit, onDelete, onOpenAsset, onOpenDomain }: { event: ApiCalendarEventOccurrence | null; busy: boolean; error: string | null; onClose: () => void; onComplete: () => void; onEdit: () => void; onDelete: () => void; onOpenAsset: () => void; onOpenDomain: () => void }) {
  useEffect(() => {
    if (!event) return
    const onKeyDown = (key: KeyboardEvent) => { if (key.key === 'Escape' && !busy) { key.preventDefault(); key.stopPropagation(); onClose() } }
    document.addEventListener('keydown', onKeyDown, true)
    return () => document.removeEventListener('keydown', onKeyDown, true)
  }, [busy, event, onClose])
  if (!event) return null
  const category = calendarCategoryPresentation[event.category]
  const domainAction = event.source === 'preventive' ? 'Abrir preventivo' : event.source === 'document' ? 'Ver documento' : null
  return <div className="fixed inset-0 z-50 flex items-start justify-center overflow-y-auto bg-slate-950/45 p-4 pt-12" role="presentation" onMouseDown={(click) => { if (click.target === click.currentTarget && !busy) onClose() }}><section role="dialog" aria-modal="true" aria-labelledby="calendar-event-details-title" className="w-full max-w-lg rounded-xl border border-slate-200 bg-white shadow-xl dark:border-slate-700 dark:bg-slate-900"><header className="flex items-start justify-between border-b border-slate-200 px-5 py-4 dark:border-slate-700"><div className="min-w-0"><span className={`inline-flex rounded px-2 py-0.5 text-xs font-medium text-white ${category.chipClass}`}>{category.label}</span><h2 id="calendar-event-details-title" className="mt-2 truncate text-lg font-semibold">{event.title}</h2></div><button type="button" aria-label="Cerrar" onClick={onClose} disabled={busy} className="rounded p-1 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800">×</button></header><div className="space-y-3 p-5 text-sm">{error && <p role="alert" className="rounded-lg border border-red-100 bg-red-50 p-3 text-red-700 dark:border-red-900/50 dark:bg-red-900/20 dark:text-red-300">{error}</p>}<dl className="grid grid-cols-[7rem_1fr] gap-y-3"><dt className="text-slate-500">Fecha</dt><dd className="capitalize">{calendarLongDate(event.date)}</dd><dt className="text-slate-500">Estado</dt><dd>{calendarStatusLabel[event.status]}</dd><dt className="text-slate-500">Origen</dt><dd>{event.sourceLabel}</dd>{event.asset && <><dt className="text-slate-500">Activo</dt><dd>{event.asset.code} · {event.asset.name}</dd><dt className="text-slate-500">Ubicación</dt><dd>{event.asset.location ?? 'Sin ubicación'}</dd></>}{event.progress && <><dt className="text-slate-500">Preventivo</dt><dd>{event.progress.completed}/{event.progress.total} tareas completadas</dd></>}</dl></div><footer className="flex flex-wrap justify-end gap-2 border-t border-slate-200 px-5 py-4 dark:border-slate-700">{event.asset && <button type="button" onClick={onOpenAsset} disabled={busy} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-800">Abrir activo</button>}{domainAction && <button type="button" onClick={onOpenDomain} disabled={busy} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-800">{domainAction}</button>}{event.canEdit && <button type="button" onClick={onEdit} disabled={busy} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-800">Editar</button>}{event.canDelete && <button type="button" onClick={onDelete} disabled={busy} className="rounded-lg border border-red-200 bg-white px-3 py-2 text-sm text-red-600 dark:border-red-900/60 dark:bg-slate-800">Eliminar</button>}{event.canComplete && <button type="button" onClick={onComplete} disabled={busy} className="rounded-lg bg-brand-600 px-3 py-2 text-sm font-medium text-white hover:bg-brand-700">{busy ? 'Completando…' : 'Completar'}</button>}</footer></section></div>
}
