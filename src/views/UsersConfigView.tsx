import { useCallback, useEffect, useState } from 'react'
import { useProject } from '@/contexts/ProjectContext'
import { addProjectMember, createManagedUser, fetchManagedUsers, fetchProjectMembers, removeProjectMember, updateManagedUser, updateProjectMember, type ApiManagedUser, type ApiProjectMember, type ApiProjectRole } from '@/lib/api'

const roles: ApiProjectRole[] = ['OWNER', 'ADMIN', 'EDITOR', 'VIEWER']
const inputClass = 'mt-1 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900'

export default function UsersConfigView() {
  const { projectId } = useProject()
  const [users, setUsers] = useState<ApiManagedUser[]>([])
  const [members, setMembers] = useState<ApiProjectMember[]>([])
  const [error, setError] = useState<string | null>(null)
  const [busy, setBusy] = useState(false)
  const [form, setForm] = useState({ name: '', email: '', password: '', initials: '', color: 'brand', isActive: true, role: 'VIEWER' as ApiProjectRole })
  const [existingId, setExistingId] = useState('')
  const [existingRole, setExistingRole] = useState<ApiProjectRole>('VIEWER')

  if (projectId === null) throw new Error('UsersConfigView requires a project scope')
  const load = useCallback(async () => {
    try { const [allUsers, projectMembers] = await Promise.all([fetchManagedUsers(projectId), fetchProjectMembers(projectId, { limit: 100 })]); setUsers(allUsers.data); setMembers(projectMembers.data); setError(null) } catch { setError('No tienes permiso para gestionar los usuarios de este proyecto.') }
  }, [projectId])
  useEffect(() => { void load() }, [load])
  const create = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); try { await createManagedUser({ ...form, projectId }); setForm({ name: '', email: '', password: '', initials: '', color: 'brand', isActive: true, role: 'VIEWER' }); await load() } catch (reason) { setError(reason instanceof Error ? reason.message : 'No se pudo crear el usuario.') } finally { setBusy(false) } }
  const addExisting = async () => { if (!existingId) return; setBusy(true); try { await addProjectMember(projectId, { userId: Number(existingId), role: existingRole }); setExistingId(''); await load() } catch (reason) { setError(reason instanceof Error ? reason.message : 'No se pudo añadir el usuario.') } finally { setBusy(false) } }
  const setRole = async (userId: number, role: ApiProjectRole) => { setBusy(true); try { await updateProjectMember(projectId, userId, role); await load() } catch (reason) { setError(reason instanceof Error ? reason.message : 'No se pudo cambiar el rol.') } finally { setBusy(false) } }
  const toggleActive = async (user: ApiManagedUser) => { setBusy(true); try { await updateManagedUser(user.id, { projectId, isActive: !user.isActive }); await load() } catch (reason) { setError(reason instanceof Error ? reason.message : 'No se pudo actualizar el usuario.') } finally { setBusy(false) } }
  const remove = async (userId: number) => { setBusy(true); try { await removeProjectMember(projectId, userId); await load() } catch (reason) { setError(reason instanceof Error ? reason.message : 'No se pudo retirar el acceso.') } finally { setBusy(false) } }
  const memberIds = new Set(members.map((member) => member.id))

  return <section className="fade-in"><div className="mb-6"><h1 className="text-2xl font-semibold tracking-tight">Usuarios y permisos</h1><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">La identidad es global; el rol se asigna de forma independiente en cada proyecto.</p></div>{error && <p role="alert" className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700 dark:border-red-900/50 dark:bg-red-950/30 dark:text-red-300">{error}</p>}<div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><div className="rounded-xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="border-b border-slate-200 px-5 py-4 dark:border-slate-800"><h2 className="font-semibold">Miembros del proyecto</h2></div><div className="divide-y divide-slate-100 dark:divide-slate-800">{members.map((member) => <div key={member.id} className="flex items-center gap-3 p-4"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100 text-xs font-semibold dark:bg-slate-800">{member.initials}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{member.name}</p><p className="truncate text-xs text-slate-500">{member.email}</p></div><select aria-label={`Rol de ${member.name}`} value={member.role} disabled={busy} onChange={(event) => void setRole(member.id, event.target.value as ApiProjectRole)} className="rounded border border-slate-200 bg-white px-2 py-1.5 text-xs dark:border-slate-700 dark:bg-slate-900">{roles.map((role) => <option key={role}>{role}</option>)}</select><button type="button" disabled={busy} onClick={() => void remove(member.id)} className="text-xs text-red-600 hover:underline disabled:opacity-50">Quitar</button></div>)}{members.length === 0 && <p className="p-5 text-sm text-slate-500">No hay miembros.</p>}</div><div className="flex gap-2 border-t border-slate-200 p-4 dark:border-slate-800"><select value={existingId} onChange={(event) => setExistingId(event.target.value)} className="min-w-0 flex-1 rounded border border-slate-200 bg-white px-2 py-2 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">Añadir usuario existente…</option>{users.filter((user) => !memberIds.has(user.id) && user.isActive).map((user) => <option key={user.id} value={user.id}>{user.name} · {user.email}</option>)}</select><select value={existingRole} onChange={(event) => setExistingRole(event.target.value as ApiProjectRole)} className="rounded border border-slate-200 bg-white px-2 py-2 text-xs dark:border-slate-700 dark:bg-slate-900">{roles.map((role) => <option key={role}>{role}</option>)}</select><button type="button" disabled={busy || !existingId} onClick={() => void addExisting()} className="rounded-lg bg-brand-600 px-3 py-2 text-sm font-medium text-white disabled:opacity-50">Añadir</button></div></div><form onSubmit={(event) => void create(event)} className="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="font-semibold">Crear usuario</h2><p className="mt-1 text-xs text-slate-500">Define una contraseña inicial de al menos 12 caracteres.</p><div className="mt-4 space-y-3"><label className="block text-sm font-medium">Nombre<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className={inputClass} /></label><label className="block text-sm font-medium">Correo<input required type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} className={inputClass} /></label><label className="block text-sm font-medium">Contraseña inicial<input required minLength={12} type="password" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} className={inputClass} /></label><div className="grid grid-cols-2 gap-3"><label className="block text-sm font-medium">Iniciales<input required maxLength={8} value={form.initials} onChange={(event) => setForm({ ...form, initials: event.target.value })} className={inputClass} /></label><label className="block text-sm font-medium">Rol<select value={form.role} onChange={(event) => setForm({ ...form, role: event.target.value as ApiProjectRole })} className={inputClass}>{roles.map((role) => <option key={role}>{role}</option>)}</select></label></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.isActive} onChange={(event) => setForm({ ...form, isActive: event.target.checked })} /> Cuenta activa al crear</label></div><button disabled={busy} className="mt-5 w-full rounded-lg bg-brand-600 px-3 py-2 text-sm font-medium text-white disabled:opacity-50">{busy ? 'Guardando…' : 'Crear y asignar'}</button></form></div><div className="mt-5 rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="font-semibold">Estado de cuentas</h2><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{users.map((user) => <div key={user.id} className="flex items-center justify-between rounded-lg border border-slate-100 p-3 dark:border-slate-800"><div><p className="text-sm font-medium">{user.name}</p><p className="text-xs text-slate-500">{user.isActive ? 'Activa' : 'Inactiva'}</p></div><button type="button" disabled={busy} onClick={() => void toggleActive(user)} className="text-xs font-medium text-brand-600 disabled:opacity-50">{user.isActive ? 'Desactivar' : 'Activar'}</button></div>)}</div></div></section>
}
