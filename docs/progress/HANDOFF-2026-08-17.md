# HANDOFF — 2026-08-17

Este es el punto de entrada para la siguiente sesión que continúe DocuCore.
No contiene secretos ni credenciales.

## Estado confirmado del repositorio

- Rama local y remota actual: `main`, limpia y sincronizada con `origin/main`.
- Último commit de `main`: `220a7f7` (`fix(prod): isolate postgres network and hostname`). Incluye además `207b97b` (`docs: add current production handoff`) y el merge de [PR #4](https://github.com/JhonyAlex/DocuCore/pull/4) (`8815f01`).
- La PR se fusionó normalmente; no se hizo push directo ni se forzaron protecciones.
- Los commits funcionales de la entrega son `bceda92`, `4ad2afd` y `ac34101`.

## Auditoría 2026-08-17 (segundo pase, por orquestador)

- **Repo**: `main` local = `origin/main` = `220a7f79b3c787ef7466e28f87e405213247afd3` (verificado por API de GitHub; el `git fetch` falló de forma transitoria por red, no por divergencia). Working tree limpio.
- **Migración** `20260817130000_workspace_manual_billing_source` presente en código y no destructiva (añade enum `BillingSource` + columna `billingSource` con `DEFAULT 'STRIPE'`).
- **Código correcto**: `server/routes/admin.ts` implementa las cuatro guardas (403 no-admin, 409 `PENDING_VERIFICATION`, 409 Starter con >1 proyecto activo, activación manual sin crear sesión Stripe y protege el entitlement manual frente a checkout/webhook); `tests/api/admin.platform.test.ts` cubre cada una.
- **CI en `main` rojo, única y exclusivamente por regresión visual**: 21/30 objetivos fuera del 0,5 % (9/30 OK). lint/typecheck/unit/build/smoke y 62 E2E no visuales pasan. El fallo es el bloqueo conocido del contrato visual, no la activación manual de planes.
- **Deploy a Dokploy `skipped` en todo el historial**: `deploy.yml` solo dispara con `workflow_run` exitoso de CI; al estar CI rojo, el despliegue automático de `main` **nunca se ha ejecutado**. Requiere despliegue manual (dispatch de `Deploy to Dokploy (Production)` desde `main`, que omite la condición de CI, o redeploy desde el panel Dokploy).
- **Producción viva**: `GET https://app.report-map.online/api/ready` → 200 `{"status":"ready","database":"connected","storage":"ok","billing":"configured","email":"configured"}` (2026-08-17T20:20Z).
- **NO verificado desde local** (requiere el agente de VPS/Dokploy): migración aplicada en la BD de producción, ciclo real de `/admin` con sesión de superadmin, y ausencia de errores Prisma/reinicios en logs.

## Entrega: activación manual de planes por superadministrador

El único usuario administrador de plataforma puede asignar manualmente a un
workspace el plan `STARTER` o `PRO`, sin crear Checkout ni requerir pago en
Stripe. La interfaz está en `/admin` y queda registrada en auditoría.

Cambios principales:

- Migración no destructiva `20260817130000_workspace_manual_billing_source`;
  añade el origen manual de facturación sin modificar datos existentes.
- `PlatformAdminView`, `PlatformWorkspaceTable` y
  `ManualPlanActivationDialog` implementan búsqueda, selección del plan y
  confirmación de la activación.
- `server/routes/admin.ts` aplica las mismas reglas de seguridad que el flujo
  de facturación:
  - rechaza activar manualmente un workspace `PENDING_VERIFICATION`; la
    verificación de correo nunca se puede eludir;
  - rechaza el plan Starter si el workspace tiene más de un proyecto activo;
  - conserva la capacidad, periodo y trazabilidad de la suscripción manual.
- `AccountView` distingue la activación manual de una suscripción de Stripe.
- `RowActionsMenu` ya no se cierra por el evento de scroll que abre el menú al
  alcanzar una tabla horizontal; las acciones posteriores de scroll sí lo
  cierran.

## Evidencia de validación de la entrega

Ejecutado en el checkout antes de fusionar:

- `pnpm lint` ✅
- `pnpm typecheck` ✅
- `pnpm build` ✅
- `pnpm test` ✅ — 55 archivos, 297 pruebas.
- `pnpm exec playwright test --grep-invert '@visual' --reporter=list` ✅ — 84 E2E.

La ejecución remota de CI de la PR fue
[32041195214](https://github.com/JhonyAlex/DocuCore/actions/runs/32041195214):
migración limpia, seed, unit/API, build, smoke y 84 E2E pasaron. Los dos
comentarios automáticos de revisión se resolvieron tras añadir las salvaguardas
anteriores.

## Pendiente prioritario: contrato visual

El contrato visual no está verde en HEAD y debe tratarse como trabajo separado:

- Localmente, `pnpm test:visual` terminó con 16/30 objetivos correctos y 14
  fuera del 0,5 %.
- En el runner Linux de CI fueron 9/30 correctos y 21 fuera del 0,5 %.
- Las desviaciones se observan en Activos, Documentos, Historial,
  Configuración y ficha de activo; en Linux también Calendario y Planos. Parte
  procede de cambios anteriores de tablas y de la variación de renderizado
  entre Windows y Linux, no de la activación manual de planes.

**Regla obligatoria:** no modificar el HTML protegido, los baselines ni el
umbral fijo del 0,5 % para ocultar el fallo. Antes de aprobar nuevos baselines
hay que inspeccionar cada captura y obtener autorización explícita del usuario.

Como `ci.yml` se ejecuta al hacer push a `main`, este fallo visual hará que el
workflow de despliegue automático lo considere no exitoso. Por ello la
publicación debe verificarse o lanzarse manualmente en Dokploy hasta que se
resuelva ese contrato.

## Operación pendiente en Dokploy y Stripe

1. Desplegar `main` en Dokploy y confirmar que la aplicación ha arrancado con
   la migración `20260817130000_workspace_manual_billing_source`.
2. Comprobar `GET https://app.report-map.online/api/ready`: debe responder 200
   con base de datos, storage, facturación y correo configurados.
3. Entrar como superadministrador en `/admin`, activar un plan manual en un
   workspace de prueba y verificar que no se crea sesión Stripe. Probar además
   que un workspace sin correo verificado recibe 409 y que Starter no permite
   más de un proyecto activo.
4. Stripe: el usuario indicó que `STRIPE_PRICE_STARTER` y `STRIPE_PRICE_PRO`
   están en modo Live y que realizó una prueba del webhook. Mantener el
   endpoint Live `https://app.report-map.online/api/billing/webhook` y los seis
   eventos listados en `docs/deployment/DOKPLOY.md`; no guardar claves ni
   secretos en Git.
5. Backups: quedan **diferidos por decisión del usuario**. Antes de una
   operación de mantenimiento, configurar y verificar una copia PostgreSQL y
   de los volúmenes `documents_data` y `floor_plans_data`. Nunca usar
   `docker compose down -v`, `db:seed` ni `db:reset:manual-test` en producción.

## Contexto de producción reportado, pendiente de revalidar

Durante la revisión del 2026-08-17 se reportaron errores Prisma `P1000` tras
un redeploy, seguidos de estabilización. Un agente con acceso a Dokploy informó
después que `/api/ready` devolvía 200 en diez llamadas consecutivas y que no
había migraciones pendientes. Es un dato histórico, no una prueba del estado
presente: volver a comprobarlo después del siguiente despliegue.

## Otros pendientes de roadmap ya existentes

- `LOC-01` continúa en revisión hasta la aceptación manual del usuario.
- `SHELL-01` conserva pendiente el selector de proyecto.
- `QA-01`: actualizar CI desde Node 20 y eliminar el aviso `DEP0205` de
  `tsx`; el runner ya avisa de la deprecación de Node 20.
- `DASH-01`, `HIST-01` y la configuración funcional están implementados, pero
  su aceptación visual debe separarse del trabajo de producto.

## Reanudación segura

Antes de modificar código, ejecutar:

```powershell
git status -sb
git fetch origin --prune
git log -1 --oneline origin/main
```

Después, revisar este archivo, `AGENTS.md`, `docs/deployment/DOKPLOY.md` y los
resultados actuales de CI/Dokploy. Las verificaciones de producción se hacen
sin borrar volúmenes ni reseedear datos.

---

## Fase RELEASE-GO-LIVE (en curso, rama `release/go-live`, PR #5 draft)

Preparación de release sin funcionalidad nueva. **No se ha desplegado a
producción** (paso separado, pendiente de GO/NO-GO).

**Commits en la rama** (no fusionados aún a `main`):
- `f3fd22f` feat(ops): `/api/version` + `/api/migrations`; `GIT_SHA`/`APP_VERSION`
  en build (`scripts/write-version.mjs`, ARG en `Dockerfile`), `/api/ready`
  incluye `version`.
- `1e036c6` fix(prod): `docker-compose.prod.yml` propaga `LEGAL_TERMS_URL`,
  `LEGAL_PRIVACY_URL` y los build args de release.
- `3d2c95c` ci: Node 22 + verificación de deploy (health/ready/SHA/migraciones).

**Nueva doc**: `docs/production/RELEASE-RUNBOOK.md` — backup, deploy, rollback y
checklist GO/NO-GO exactos.

**Contrato visual (BLOQUEANTE, sin resolver)**: 21/30 fuera de umbral en Linux.
Clasificación por evidencia de historial (commits posteriores a los baselines
RELEASE-01 del 2026-08-12): las 7 vistas que fallan son CAMBIO_INTENCIONAL
(ordenación de columnas, 5 imágenes/activo, colores de tipo, tipos documentales,
truncado de filas, multi-proyecto). `history` es la única vista protegida (HTML)
afectada: necesita decisión de producto (revertir o reclasificar como evolucionada
con baseline). **No se regeneran baselines sin inspección visual y aprobación
explícita del usuario.**

**Limitación de la sesión**: el modelo no dispone de visión de imágenes (el visor
devuelve `[Unsupported Image]`), por lo que la inspección pixel a pixel de las 21
capturas no pudo hacerse en local; la clasificación se apoya en métricas de CI,
historial de commits y localización de diffs. Docker local no está disponible, así
que la suite visual debe validarse en el runner Linux de CI.

**Próximo paso**: inspeccionar/aprobar las capturas y, con autorización, regenerar
los 6 baselines evolucionados en Linux y decidir el caso `history`. Después,
re-ejecutar CI hasta 30/30 y ejecutar el runbook de deploy.

